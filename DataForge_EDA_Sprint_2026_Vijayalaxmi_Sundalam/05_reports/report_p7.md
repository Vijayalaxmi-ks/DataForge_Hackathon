# Practical 7 - Python DataFrame Setup & Schema Validation
**Student ID:** 2026CS017
**Name:** Vijayalaxmi Sundalam
**Dataset:** Superstore Sales Dataset  

## Problem Statement
Programmatically load, validate, and isolate data matrix structures that scale beyond traditional spreadsheet grid bounds without inducing compute crashes.

## Procedure
1. Initialized an isolated runtime workspace using Python virtual environments (`.venv`) and deployed core data structures via `pandas`.
2. Loaded raw source vectors from the data path into a highly optimized Vectorized Data Structure (`df`).
3. Executed baseline internal checks using `df.head()`, `df.info()`, and `df.describe()` to capture memory allocation shapes.
4. Programmatically constructed a customized schema diagnostic auditing framework checking feature types, null margins, and unique counts.
5. Persisted the generated audit tracing matrix down to disk as `schema_audit.csv`.

## Report Questions
* **Q1. What is the difference between a column and a measure?** In a programming context, a column represents an entire row-indexed contiguous array vector instantiated statically in memory. A measure is an evaluation expression built to calculate aggregates dynamically over a changing data subset context on demand.
* **Q2. Which KPI would you put on top of the dashboard and why?** **Profit Margin** belongs at the apex of an analytical stack. Monitoring standalone gross volume trends can obscure internal resource drains; mapping true margin percentages guarantees real-time visibility into overall structural stability and systemic operational health.

## Conclusion
Transitioning analysis to memory-optimized programmatic dataframes replaces slow, manual grid inspections with high-speed, repeatable schema auditing arrays.